# Gate 10 branch note

Gate 10 tests a CausalHorizon-inspired coverage hypothesis without changing Gate 9's historical receipts.

The classifier does not expose the exact event-entry map `Q`. Gate 10 therefore measures, for every stored cue, a finite response signature under a fixed panel of temporary parameter probes and selects a 20-record bank for global signature-space volume/conditioning.

See `GATE10_PROTOCOL.md`, `gate10_q_coverage.py`, and `GATE10_FINDINGS.md`.
